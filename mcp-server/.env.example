# Adobe Target Admin API Configuration
# Copy this file to .env and update with your actual credentials

# ===== AUTHENTICATION =====
# Your Adobe Target Tenant ID (e.g., "yourcompany")
TARGET_TENANT_ID=

# Your Adobe Target API Key (Client ID from Adobe Developer Console)
TARGET_API_KEY=

# Your Adobe Target Access Token (Bearer token from Adobe IMS)
# This is the token that expires and needs to be refreshed
TARGET_ACCESS_TOKEN=

# Optional: Specify a workspace ID to filter operations to a specific workspace
# Leave empty to work with all workspaces
TARGET_WORKSPACE_ID=

# ===== DEFAULT VALUES FOR ACTIVITIES =====
# These values will be used as defaults when creating activities/offers
# You can override them on a per-call basis

# Default mbox name for activities (comma-separated for multiple)
# Examples:
#   Single mbox: target-global-mbox
#   Multiple: target-global-mbox,homepage-mbox,checkout-mbox
TARGET_DEFAULT_MBOXES=target-global-mbox

# Default activity priority (0-999)
# Lower numbers = lower priority, higher numbers = higher priority
TARGET_DEFAULT_PRIORITY=5

# Default visitor percentage for entry constraints (0-100)
# 100 = all visitors can enter, 50 = only 50% can enter
TARGET_DEFAULT_VISITOR_PERCENTAGE=100

# ===== ANALYTICS FOR TARGET (A4T) DEFAULTS =====
# Configure these if you want automatic A4T integration for all activities

# Analytics tracking server (e.g., company.sc.omtrdc.net)
# Find this in Analytics > Admin > Report Suites > Edit Settings > General > Tracking Server
TARGET_A4T_DATA_COLLECTION_HOST=

# Analytics company name
# This is your Adobe Analytics company/login company name
TARGET_A4T_COMPANY_NAME=

# Analytics report suite IDs (comma-separated for multiple)
# Examples:
#   Single: prod-rsid
#   Multiple: prod-rsid,dev-rsid,staging-rsid
TARGET_A4T_REPORT_SUITES=

# ===== SUCCESS METRICS DEFAULTS =====
# Default metric type: "engagement" for page views (safest, always works), "conversion" for mbox tracking
# engagement = tracks page views, time on site, or custom scoring (no mbox required)
# conversion = tracks specific conversion events via mbox (requires implementation)
TARGET_DEFAULT_METRIC_TYPE=engagement

# Default engagement metric (only used if metric type is "engagement")
# Options: page_count (page views), time_on_site, score, none
TARGET_DEFAULT_ENGAGEMENT_METRIC=page_count

# Default metric action type
# Options: count_once, count_landings, always_convert, restart_same_experience,
#          restart_random_experience, restart_new_experience,
#          exclude_to_same_experience, ban_from_campaign
TARGET_DEFAULT_METRIC_ACTION=count_once

# Optional: Default mbox for conversion tracking (only used if metric type is "conversion")
# This mbox should fire when a conversion event occurs
TARGET_DEFAULT_SUCCESS_MBOX=registration

# Optional: Default success event type (only used if metric type is "conversion")
# Options: mbox_shown, mbox_clicked
TARGET_DEFAULT_SUCCESS_EVENT=mbox_shown
